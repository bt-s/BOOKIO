// import React from 'react';
// import PropTypes from 'prop-types';

// const Toggle = props => {
//   [pushed, setPushed] = useState(false);

//   return (
//     <button className={pushed ? 'toggle-filter-down' : 'toggle-filter-up'}>
//       {props.title}
//     </button>
//   );
// };

// Toggle.propTypes = {
//   title: PropTypes.string
// };

// export default Toggle;
